- name: variables in ansible
  hosts: local
  connection: local
  vars:
    name: murali
    age: 20
  tasks:
    - name: locally
      vars:
        name: kinod
        anisble.builtin.debug:
          msg: "{{ name }} and {{ age }}"