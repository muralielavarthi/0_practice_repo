- name: ping the server
  hosts: local
  connection: local
  tasks:
    - name: ping new server
      ansible.builtin.ping:
    - name: hello the server
      ansible.builtin.debug:
        msg: "hello server"
    - name: create a dir
      ansible.builtin.file:
        path: "/home/ec2-user/dir"
        state: directory
        mode: 666
    - name: create a file
      ansible.builtin.file:
        path: "/home/ec2-user/f1"
        mode: 421
