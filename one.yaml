- name: varibale types
  hosts: local
  connection: local
  vars:
    Name: Mutrali
    prof: enginerr
  tasks:
    - name: print locally
      vars:
        Name: Muralie
        prof: minister
      ansible.builtin.debug:
        msg: "{{ Name }} and {{ prof }}"