- name: data types
  hosts: local
  connection: local
  tasks:
    - name: data types in ansible
      vars:
        user: "murali"
        age: 20
        techstack:
          - shell
          - ansible
        tech:
          - { name: murali, city: bng }
          - { name: kindk, city: hyd }
      ansible.builtin.debug:
        msg: " {{ user }} and {{ age }} and {{ techstack }} and {{ tech }}"
